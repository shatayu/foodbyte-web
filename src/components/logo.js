const colors = require("../style").default.COLORS;

const style = {
  fontSize: '4.75em',
  fontWeight: 'bold',
  fontFamily: 'Helvetica',
  color: colors.YELLOW,
  textAlign: 'center'
};
export default style;
